{
  "name": "playwright-e2e",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "playwright test && npx playwright show-report",
    "test:frontend": "playwright test --project=frontend-* && npx playwright show-report",
    "test:api": "playwright test --project=api && npx playwright show-report",
    "test:all": "playwright test && npx playwright show-report",
    "test:report": "npx playwright show-report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/mcp": "^0.0.55",
    "@playwright/test": "^1.57.0",
    "@types/node": "^25.0.6"
  }
}
